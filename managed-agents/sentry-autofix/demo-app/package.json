{
  "name": "sentry-autofix-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env --import ./src/instrument.js src/server.js",
    "test": "node --test",
    "break": "node scripts/break.js"
  },
  "dependencies": {
    "@sentry/node": "^10",
    "express": "^5"
  }
}
