sandbox_name: sandbox-reasonable-mind-v0
mode: sandbox
created_from_branch: sandbox/sandbox-reasonable-mind-v0
created_at: "2025-12-05T08:40:00Z"

agent_constraints:
  allowed_paths:
    - "experiments/sandbox-reasonable-mind-v0/**"
  forbidden_paths:
    - "agents/core/**"
    - ".git/**"
    - "experiments/**/venv/**"
  rules:
    - "Do not modify files outside experiments/sandbox-reasonable-mind-v0."
    - "Do not push commits to remote without explicit human instruction."
    - "All autonomous changes must be committed with clear messages."

notes:
  purpose: "Safe, isolated environment for experimenting with ReasonableMindEngine, profiles, and coding agents."
  parent_branch: "wgt-test-dev"
  python_version: "3.14"
