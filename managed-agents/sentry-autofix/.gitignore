node_modules/
.env
.env.local
package-lock.json
# Written by `ant apply`: the IDs of the agent and environment it created in
# your organization. In your own project, commit it so your team shares them.
claude-lock.json
